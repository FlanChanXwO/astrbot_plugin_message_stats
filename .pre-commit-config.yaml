repos:
  - repo: local
    hooks:
      - id: t2i-renderer-tests
        name: T2I renderer regression tests
        entry: python3 -m unittest discover -s tests
        language: system
        pass_filenames: false
